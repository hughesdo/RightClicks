{
  "model_id": "fal-ai/wan-flf2v",
  "display_name": "Wan FLF2V (Original)",
  "description": "Wan First+Last Frame to Video - Original model for smooth transitions",
  "pricing": "$0.20/video",
  "processing_time": "~2-3 minutes",
  "image_param_names": {
    "start": "start_image_url",
    "end": "end_image_url"
  },
  "parameters": {
    "prompt": {
      "type": "text",
      "label": "Prompt",
      "description": "Text prompt to guide video generation",
      "required": true,
      "default": "A smooth cinematic transition between the two frames",
      "multiline": true,
      "rows": 3,
      "clear_on_focus": true
    },
    "negative_prompt": {
      "type": "text",
      "label": "Negative Prompt",
      "description": "What to avoid in the generation",
      "required": false,
      "default": "bright colors, overexposed, static, blurred details",
      "multiline": true,
      "rows": 2,
      "clear_on_focus": false
    },
    "num_frames": {
      "type": "slider",
      "label": "Number of Frames",
      "description": "Total frames (more frames = longer video, higher cost)",
      "required": false,
      "default": 81,
      "min": 41,
      "max": 161,
      "step": 1
    },
    "frames_per_second": {
      "type": "slider",
      "label": "Frames Per Second",
      "description": "Video frame rate",
      "required": false,
      "default": 16,
      "min": 8,
      "max": 30,
      "step": 1
    },
    "resolution": {
      "type": "dropdown",
      "label": "Resolution",
      "description": "Output video resolution",
      "required": false,
      "options": ["480p", "720p", "1080p"],
      "default": "720p"
    },
    "aspect_ratio": {
      "type": "dropdown",
      "label": "Aspect Ratio",
      "description": "Video aspect ratio (auto = derived from input images)",
      "required": false,
      "options": ["auto", "16:9", "9:16", "1:1"],
      "default": "auto"
    },
    "seed": {
      "type": "number",
      "label": "Seed",
      "description": "Random seed for reproducibility (leave empty for random)",
      "required": false,
      "default": "Random",
      "min": 0,
      "max": 2147483647,
      "step": 1
    }
  }
}

