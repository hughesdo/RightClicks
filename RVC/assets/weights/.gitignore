# RVC voice models are stored as compressed .zip files to save space
# install.bat extracts them to .pth files during installation

# Ignore extracted .pth files (generated during install)
*.pth

# Keep this gitignore
!.gitignore

# Include compressed model files
!*.zip
