<UserControl x:Class="RightClicksClipEditor.Controls.TimelineControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800"
             Background="#1E1E1E">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/> <!-- Timecode labels -->
            <RowDefinition Height="*"/>  <!-- Timeline track -->
            <RowDefinition Height="20"/> <!-- Zoom controls -->
        </Grid.RowDefinitions>
        
        <!-- Timecode Labels -->
        <Canvas x:Name="TimecodeCanvas" Grid.Row="0" Background="#2D2D2D" ClipToBounds="True">
            <!-- Timecode labels will be drawn here dynamically -->
        </Canvas>
        
        <!-- Timeline Track -->
        <Border Grid.Row="1" Background="#2D2D2D" BorderBrush="#3E3E3E" BorderThickness="0,1">
            <Canvas x:Name="TimelineCanvas" ClipToBounds="True"
                    MouseLeftButtonDown="TimelineCanvas_MouseLeftButtonDown"
                    MouseMove="TimelineCanvas_MouseMove"
                    MouseLeftButtonUp="TimelineCanvas_MouseLeftButtonUp"
                    MouseWheel="TimelineCanvas_MouseWheel">
                
                <!-- Selection highlight (between IN and OUT) -->
                <Rectangle x:Name="SelectionHighlight" 
                           Fill="#4CAF5040" 
                           Stroke="#4CAF50" 
                           StrokeThickness="1"
                           Visibility="Collapsed"/>
                
                <!-- IN Marker -->
                <Canvas x:Name="InMarker" Visibility="Collapsed" Cursor="SizeWE">
                    <Polygon Points="0,0 10,0 5,8" Fill="#4CAF50" Canvas.Top="0"/>
                    <Rectangle Width="2" Height="1000" Fill="#4CAF50" Canvas.Left="4" Canvas.Top="8"/>
                    <TextBlock x:Name="InMarkerLabel" Text="IN" Foreground="White" FontSize="10" 
                               FontWeight="Bold" Canvas.Left="-8" Canvas.Top="10" Background="#4CAF50" Padding="2"/>
                </Canvas>
                
                <!-- OUT Marker -->
                <Canvas x:Name="OutMarker" Visibility="Collapsed" Cursor="SizeWE">
                    <Polygon Points="0,0 10,0 5,8" Fill="#F44336" Canvas.Top="0"/>
                    <Rectangle Width="2" Height="1000" Fill="#F44336" Canvas.Left="4" Canvas.Top="8"/>
                    <TextBlock x:Name="OutMarkerLabel" Text="OUT" Foreground="White" FontSize="10" 
                               FontWeight="Bold" Canvas.Left="-10" Canvas.Top="10" Background="#F44336" Padding="2"/>
                </Canvas>
                
                <!-- Playhead -->
                <Canvas x:Name="Playhead" Cursor="SizeWE">
                    <Polygon Points="0,0 10,0 5,8" Fill="#2196F3" Canvas.Top="0"/>
                    <Rectangle Width="2" Height="1000" Fill="#2196F3" Canvas.Left="4" Canvas.Top="8"/>
                    <TextBlock x:Name="PlayheadLabel" Text="00:00:00.000" Foreground="White" FontSize="10" 
                               FontWeight="Bold" Canvas.Left="-25" Canvas.Top="10" Background="#2196F3" Padding="2"/>
                </Canvas>
            </Canvas>
        </Border>
        
        <!-- Zoom Controls -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Background="#252525">
            <TextBlock Text="Zoom:" Foreground="#999" VerticalAlignment="Center" Margin="5,0"/>
            <Button Content="-" Width="20" Height="18" Margin="5,0" Click="ZoomOut_Click" 
                    ToolTip="Zoom out (Ctrl+Mouse Wheel)"/>
            <TextBlock x:Name="ZoomLevelText" Text="100%" Foreground="White" VerticalAlignment="Center" 
                       MinWidth="40" TextAlignment="Center"/>
            <Button Content="+" Width="20" Height="18" Margin="5,0" Click="ZoomIn_Click" 
                    ToolTip="Zoom in (Ctrl+Mouse Wheel)"/>
            <Button Content="Fit" Width="30" Height="18" Margin="10,0,5,0" Click="ZoomFit_Click" 
                    ToolTip="Fit entire duration"/>
        </StackPanel>
    </Grid>
</UserControl>

